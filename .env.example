# OpenAI API Key - Required for document analysis and search
OPENAI_API_KEY=your_openai_api_key_here

# Frontend API URL - URL where the backend API is running
FRONTEND_API_URL=http://localhost:3000

# Backend API URL - URL where the backend API is running (for frontend)
NEXT_PUBLIC_API_URL=http://localhost:8000
